# cc-reader 界面灵动化改造 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 cc-reader 加上亮/暗双主题、四个动画、侧栏选中态、工具调用显示（含可折叠输出）、LaTeX 公式渲染，同时保持现有信息密度与最小改动。

**Architecture:** 用一套语义化 CSS 变量（`--c-*`）承载亮/暗两套配色，Tailwind 注册为语义色类，组件从写死颜色迁移到语义色；主题通过根元素 `.dark` class 切换，`useTheme` hook 管理状态与持久化。工具调用通过「接上已断的线」实现——后端 parser 已提取 `toolCalls`，补上 `tool_result` 配对，前端 `AssistantMessage` 渲染现有 `ToolCallBlock`。公式渲染接入 KaTeX 到现有 react-markdown 管线，并预处理反引号包裹的公式。

**Tech Stack:** React 18 + Vite + Tailwind 3 + TypeScript；Express 后端 parser；新增 `katex` / `remark-math` / `rehype-katex`。

**验证策略:** 项目无测试框架。本计划不引入测试框架（符合最小改动原则）。纯逻辑改动（parser 配对、反引号解包）用针对真实 JSONL 数据的临时 Node 脚本验证后删除；视觉改动用 `npm run dev` + Chrome DevTools 截图肉眼验证。

---

## 颜色语义映射（全局参照表）

CSS 变量定义（写入 `src/index.css`）：

| 语义变量 | 亮色值 | 暗色值 | 用途 |
|---|---|---|---|
| `--c-base` | `#ffffff` | `#0f1117` | 主内容区背景 |
| `--c-side` | `#fafafb` | `#13161e` | 侧栏/顶栏/Tab 背景 |
| `--c-surface` | `#ffffff` | `rgba(255,255,255,.045)` | 卡片/assistant 气泡 |
| `--c-edge` | `#ececf0` | `rgba(255,255,255,.09)` | 边框/分隔线 |
| `--c-ink` | `#1f2937` | `#e2e8f0` | 主文字 |
| `--c-dim` | `#9aa1ad` | `#6b7689` | 次要文字 |
| `--c-accent` | `#6366f1` | `#22d3ee` | 强调色 |
| `--c-accent-soft` | `#eef0ff` | `rgba(34,211,238,.12)` | 强调色浅背景 |
| `--c-sel-bg` | `#f0f2ff` | `rgba(34,211,238,.13)` | 选中项背景 |
| `--c-sel-ink` | `#5b54e6` | `#5eead4` | 选中项文字 |

Tailwind 语义色类（注册到 `tailwind.config.js`）：`bg-base` `bg-side` `bg-surface` `border-edge` `text-ink` `text-dim` `text-accent` `bg-accent` `bg-accent-soft` `bg-sel-bg` `text-sel-ink`。

---

## Task 1: 安装依赖

**Files:**
- Modify: `package.json`（由 npm 自动写入）

- [ ] **Step 1: 安装公式渲染依赖**

Run:
```bash
npm install katex remark-math rehype-katex
```
Expected: 三个包加入 `dependencies`，无报错。

- [ ] **Step 2: 确认安装成功**

Run: `node -e "require('katex'); require('remark-math'); require('rehype-katex'); console.log('ok')"`
Expected: 输出 `ok`

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: add katex, remark-math, rehype-katex for formula rendering"
```

---

## Task 2: 主题地基（CSS 变量 + Tailwind 语义色 + 防闪脚本）

这步完成后，主题变量就位、Tailwind 语义色可用、刷新不白闪。组件换色在后续任务。

**Files:**
- Modify: `tailwind.config.js`
- Modify: `src/index.css`
- Modify: `index.html`

- [ ] **Step 1: Tailwind 开启 class 暗色模式 + 注册语义色**

将 `tailwind.config.js` 整体替换为：
```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        base: "var(--c-base)",
        side: "var(--c-side)",
        surface: "var(--c-surface)",
        edge: "var(--c-edge)",
        ink: "var(--c-ink)",
        dim: "var(--c-dim)",
        accent: "var(--c-accent)",
        "accent-soft": "var(--c-accent-soft)",
        "sel-bg": "var(--c-sel-bg)",
        "sel-ink": "var(--c-sel-ink)",
      },
    },
  },
  plugins: [],
};
```

- [ ] **Step 2: 在 index.css 顶部定义亮/暗变量**

在 `src/index.css` 的 `@tailwind utilities;` 之后、`:root { --font-size }` 处，把 `:root` 块替换为下面内容（保留 `--font-size`）：
```css
:root {
  --font-size: 16px;

  --c-base: #ffffff;
  --c-side: #fafafb;
  --c-surface: #ffffff;
  --c-edge: #ececf0;
  --c-ink: #1f2937;
  --c-dim: #9aa1ad;
  --c-accent: #6366f1;
  --c-accent-soft: #eef0ff;
  --c-sel-bg: #f0f2ff;
  --c-sel-ink: #5b54e6;
}

.dark {
  --c-base: #0f1117;
  --c-side: #13161e;
  --c-surface: rgba(255, 255, 255, 0.045);
  --c-edge: rgba(255, 255, 255, 0.09);
  --c-ink: #e2e8f0;
  --c-dim: #6b7689;
  --c-accent: #22d3ee;
  --c-accent-soft: rgba(34, 211, 238, 0.12);
  --c-sel-bg: rgba(34, 211, 238, 0.13);
  --c-sel-ink: #5eead4;
}

html, body {
  background: var(--c-base);
}
```

- [ ] **Step 3: index.html 加防白闪脚本 + 根背景**

将 `index.html` 的 `<head>` 末尾（`</head>` 前）加入：
```html
    <script>
      (function () {
        try {
          var t = localStorage.getItem("cc-reader-theme");
          var dark = t ? t === "dark" : window.matchMedia("(prefers-color-scheme: dark)").matches;
          if (dark) document.documentElement.classList.add("dark");
        } catch (e) {}
      })();
    </script>
```

- [ ] **Step 4: 验证构建**

Run: `npm run build`
Expected: 构建成功，无 TS/Tailwind 报错。

- [ ] **Step 5: Commit**

```bash
git add tailwind.config.js src/index.css index.html
git commit -m "feat: add theme CSS variables, tailwind semantic colors, anti-flash script"
```

---

## Task 3: useTheme hook + 顶栏主题开关

**Files:**
- Create: `src/hooks/useTheme.ts`
- Modify: `src/components/Toolbar.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: 创建 useTheme hook**

写入 `src/hooks/useTheme.ts`（模式参照现有 `useFontSize.ts`）：
```ts
import { useState, useEffect, useCallback } from "react";

const STORAGE_KEY = "cc-reader-theme";

type Theme = "light" | "dark";

function getInitial(): Theme {
  try {
    const stored = localStorage.getItem(STORAGE_KEY);
    if (stored === "light" || stored === "dark") return stored;
  } catch { /* ignore */ }
  try {
    return window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
  } catch { /* ignore */ }
  return "light";
}

export function useTheme() {
  const [theme, setTheme] = useState<Theme>(getInitial);

  useEffect(() => {
    const root = document.documentElement;
    if (theme === "dark") root.classList.add("dark");
    else root.classList.remove("dark");
    try {
      localStorage.setItem(STORAGE_KEY, theme);
    } catch { /* ignore */ }
  }, [theme]);

  const toggle = useCallback(() => {
    setTheme((t) => (t === "dark" ? "light" : "dark"));
  }, []);

  return { theme, isDark: theme === "dark", toggle };
}
```

- [ ] **Step 2: Toolbar 接收主题 props 并渲染滑动开关**

将 `src/components/Toolbar.tsx` 整体替换为（同时把现有颜色换成语义色）：
```tsx
interface Props {
  title: string;
  fontSize: number;
  onIncrease: () => void;
  onDecrease: () => void;
  connected: boolean;
  isDark: boolean;
  onToggleTheme: () => void;
}

export default function Toolbar({
  title,
  fontSize,
  onIncrease,
  onDecrease,
  connected,
  isDark,
  onToggleTheme,
}: Props) {
  return (
    <div className="h-12 bg-side border-b border-edge px-4 flex items-center gap-3 shrink-0 transition-colors">
      <h2 className="text-sm font-medium text-ink truncate flex-1">
        {title || "CC Reader"}
      </h2>

      <div className="flex items-center gap-1">
        <button
          onClick={onDecrease}
          className="px-2 py-1 text-sm bg-accent-soft hover:bg-accent-soft text-ink rounded transition-all hover:-translate-y-0.5"
          title="Decrease font size (Ctrl+-)"
        >
          A−
        </button>
        <span className="text-xs text-dim w-10 text-center">{fontSize}px</span>
        <button
          onClick={onIncrease}
          className="px-2 py-1 text-sm bg-accent-soft hover:bg-accent-soft text-ink rounded transition-all hover:-translate-y-0.5"
          title="Increase font size (Ctrl+=)"
        >
          A+
        </button>
      </div>

      <button
        onClick={onToggleTheme}
        className="relative w-12 h-6 rounded-full bg-accent-soft border border-edge transition-colors shrink-0"
        title={isDark ? "Switch to light" : "Switch to dark"}
      >
        <span
          className={`absolute top-0.5 left-0.5 w-[18px] h-[18px] rounded-full bg-accent flex items-center justify-center text-[10px] transition-transform duration-300 ${
            isDark ? "translate-x-6 shadow-[0_0_8px_1px_var(--c-accent)]" : ""
          }`}
        >
          {isDark ? "🌙" : "☀"}
        </span>
      </button>

      <div
        className={`w-2 h-2 rounded-full ${connected ? "bg-green-400" : "bg-red-400"}`}
        title={connected ? "Connected" : "Disconnected"}
      />
    </div>
  );
}
```

- [ ] **Step 3: App 接线 useTheme 并传给 Toolbar**

在 `src/App.tsx`：
1. 加 import：`import { useTheme } from "./hooks/useTheme";`
2. 在 `useFontSize` 那行附近加：`const { isDark, toggle: toggleTheme } = useTheme();`
3. 把最外层 `<div className="flex h-screen bg-white text-gray-900">` 改为 `<div className="flex h-screen bg-base text-ink transition-colors">`
4. 给 `<Toolbar .../>` 增加两个 props：`isDark={isDark}` 和 `onToggleTheme={toggleTheme}`

- [ ] **Step 4: 验证主题切换**

Run: `npm run dev`（后台），用 Chrome DevTools 打开 `http://localhost:5173`，点顶栏开关。
Expected: 整页背景在白色与深色 `#0f1117` 间平滑过渡，开关旋钮滑动，刷新后保持选择。

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useTheme.ts src/components/Toolbar.tsx src/App.tsx
git commit -m "feat: add useTheme hook and toolbar theme toggle switch"
```

---

## Task 4: 换肤 — 布局骨架（Sidebar / TabBar / QuestionNav）

把这三个组件的写死颜色迁移到语义色。逐类替换，不改结构。

**Files:**
- Modify: `src/components/Sidebar.tsx`
- Modify: `src/components/TabBar.tsx`
- Modify: `src/components/QuestionNav.tsx`

- [ ] **Step 1: Sidebar 换色**

在 `src/components/Sidebar.tsx` 按下表全局替换 class（每处出现都替换）：

| 原 class | 新 class |
|---|---|
| `bg-gray-50` | `bg-side` |
| `border-gray-200` | `border-edge` |
| `text-gray-500` | `text-dim` |
| `text-gray-400` | `text-dim` |
| `text-gray-600` | `text-ink` |
| `text-gray-800` | `text-ink` |
| `hover:bg-gray-200` | `hover:bg-accent-soft` |
| `hover:bg-gray-200/60` | `hover:bg-accent-soft` |
| `hover:bg-gray-200/60` (TreeNode) | `hover:bg-accent-soft` |

另外：
- 折叠态与展开态最外层 `div` 末尾加 `transition-colors`。
- 顶部标题 `<h1 className="text-sm font-bold text-gray-800">` → `text-ink`。
- "Loading projects..." 容器同样把 `bg-gray-50 / text-gray-500` 换为 `bg-side / text-dim`。

- [ ] **Step 2: TabBar 换色**

在 `src/components/TabBar.tsx`：
- 容器 `bg-gray-100 border-b border-gray-200` → `bg-side border-b border-edge`
- 激活 Tab：`bg-white text-gray-900 border-b-2 border-b-blue-500 font-medium` → `bg-base text-ink border-b-2 border-b-accent font-medium`
- 非激活 Tab：`bg-gray-50 text-gray-500 hover:bg-gray-100 hover:text-gray-700` → `bg-side text-dim hover:bg-accent-soft hover:text-ink`
- 关闭按钮：`hover:bg-gray-300/60 text-gray-400 hover:text-gray-700` → `hover:bg-accent-soft text-dim hover:text-ink`
- 每个 Tab 的外层 `className` 已含 `transition-colors`，保留。

- [ ] **Step 3: QuestionNav 换色**

在 `src/components/QuestionNav.tsx`：
- 折叠态容器 `bg-gray-50 border-l border-gray-200` → `bg-side border-l border-edge`，按钮 `text-gray-500 hover:text-blue-600 hover:bg-gray-200` → `text-dim hover:text-accent hover:bg-accent-soft`，数字徽标 `text-blue-500 bg-blue-100` → `text-accent bg-accent-soft`
- 展开态容器 `bg-gray-50 border-l border-gray-200` → `bg-side border-l border-edge`
- 头部 `border-gray-200`、`text-gray-500`、`text-gray-400 hover:text-gray-600` → `border-edge`、`text-dim`、`text-dim hover:text-ink`
- 搜索框 `bg-white border-gray-300 text-gray-700 placeholder-gray-400 focus:ring-blue-400` → `bg-base border-edge text-ink placeholder-dim focus:ring-accent`
- 列表项 `text-gray-600 hover:bg-blue-50 hover:text-gray-900 border-gray-100` → `text-ink hover:bg-accent-soft border-edge`，`#index` 的 `text-blue-600 ... group-hover:text-blue-700` → `text-accent`，文字 `text-gray-700 group-hover:text-gray-900` → `text-ink`

- [ ] **Step 4: 验证**

Run: `npm run dev`，DevTools 截图亮色与暗色两种状态。
Expected: 三栏骨架在两种主题下均正常，无残留白底/黑字撞色。

- [ ] **Step 5: Commit**

```bash
git add src/components/Sidebar.tsx src/components/TabBar.tsx src/components/QuestionNav.tsx
git commit -m "feat: migrate layout components to semantic theme colors"
```

---

## Task 5: 换肤 — 消息区（UserMessage / AssistantMessage / MessageList）

**Files:**
- Modify: `src/components/UserMessage.tsx`
- Modify: `src/components/AssistantMessage.tsx`
- Modify: `src/components/MessageList.tsx`

- [ ] **Step 1: UserMessage 换色**

将 `src/components/UserMessage.tsx` 的渲染部分替换为（仅改 class，逻辑不动）：
```tsx
  return (
    <div
      id={`msg-${message.uuid}`}
      className="border-l-4 border-accent bg-accent-soft rounded-r-lg p-4 my-3 transition-all"
      style={{ fontSize: "var(--font-size)" }}
    >
      <div className="flex items-center gap-2 mb-2">
        <span className="text-xs font-bold text-white bg-accent px-2 py-0.5 rounded">
          You
        </span>
        {questionIndex !== undefined && (
          <span className="text-xs text-dim">#{questionIndex}</span>
        )}
        <span className="text-xs text-dim ml-auto">
          {new Date(message.timestamp).toLocaleTimeString()}
        </span>
      </div>
      <div className="prose max-w-none">
        <MarkdownContent content={text} />
      </div>
    </div>
  );
```

- [ ] **Step 2: AssistantMessage 换色（本任务仅换色，工具调用在 Task 8）**

将 `src/components/AssistantMessage.tsx` 渲染部分的 class 改为：
- 最外层 `className="rounded-lg p-4 my-3 transition-all"` → `className="bg-surface border border-edge rounded-lg p-4 my-3 transition-all"`
- `Claude` 徽标：`text-purple-600 bg-purple-100` → `text-accent bg-accent-soft`
- 两处 `text-gray-400` → `text-dim`

- [ ] **Step 3: prose 暗色适配**

在 `src/index.css` 末尾追加暗色 prose 覆盖：
```css
.dark .prose {
  color: var(--c-ink);
}
.dark .prose h1,
.dark .prose h2,
.dark .prose h3,
.dark .prose h4,
.dark .prose strong {
  color: #f1f5f9;
}
.dark .prose blockquote {
  color: var(--c-dim);
  border-left-color: var(--c-edge);
}
.dark .prose hr {
  border-color: var(--c-edge);
}
.dark .prose a {
  color: var(--c-accent);
}
```

- [ ] **Step 4: MessageList 空态换色**

`src/components/MessageList.tsx`：两处 `text-gray-400` → `text-dim`。

- [ ] **Step 5: 验证 + Commit**

Run: `npm run dev`，截图亮/暗下的对话区。
Expected: user 气泡用强调色边/底，assistant 气泡有表面色与边框，暗色文字清晰。
```bash
git add src/components/UserMessage.tsx src/components/AssistantMessage.tsx src/components/MessageList.tsx src/index.css
git commit -m "feat: migrate message components to semantic theme colors"
```

---

## Task 6: 四个动画 + reduced-motion

**Files:**
- Modify: `src/index.css`
- Modify: `src/components/MessageList.tsx`

- [ ] **Step 1: 在 index.css 定义动画**

在 `src/index.css` 末尾追加：
```css
/* 消息进场淡入上滑 */
@keyframes cc-msg-in {
  from { opacity: 0; transform: translateY(12px); }
  to { opacity: 1; transform: translateY(0); }
}
.cc-msg-in {
  animation: cc-msg-in 0.45s cubic-bezier(0.2, 0.8, 0.2, 1) both;
}

/* 连接指示灯呼吸脉冲（绿色已连接时） */
@keyframes cc-pulse {
  0% { box-shadow: 0 0 0 0 rgba(52, 211, 153, 0.5); }
  70% { box-shadow: 0 0 0 6px rgba(52, 211, 153, 0); }
  100% { box-shadow: 0 0 0 0 rgba(52, 211, 153, 0); }
}
.cc-pulse {
  animation: cc-pulse 1.8s infinite;
}

/* 尊重无障碍：减少动效 */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

注：hover 微浮起已通过各组件的 `transition-all hover:-translate-y-0.5`（按钮）/ `hover:translate-x-...`（侧栏项，Task 7）实现；主题切换渐变已通过 `transition-colors` 实现。

- [ ] **Step 2: MessageList 给每条消息套进场动画**

在 `src/components/MessageList.tsx`，把消息 `map` 内的 `UserMessage` / `AssistantMessage` 各用一个带 `className="cc-msg-in"` 的包裹 div 包住（key 移到外层 div）：
```tsx
      {messages.map((msg) => {
        if (msg.type === "user") {
          return (
            <div key={msg.uuid} className="cc-msg-in">
              <UserMessage
                message={msg}
                questionIndex={userQuestionIndices.get(msg.uuid)}
              />
            </div>
          );
        }
        return (
          <div key={msg.uuid} className="cc-msg-in">
            <AssistantMessage message={msg} />
          </div>
        );
      })}
```

- [ ] **Step 3: 连接指示灯加脉冲**

在 `src/components/Toolbar.tsx`，把连接指示灯那行改为（仅已连接时脉冲）：
```tsx
      <div
        className={`w-2 h-2 rounded-full ${connected ? "bg-green-400 cc-pulse" : "bg-red-400"}`}
        title={connected ? "Connected" : "Disconnected"}
      />
```

- [ ] **Step 4: 验证 + Commit**

Run: `npm run dev`，打开一个会话观察消息淡入；确认连接灯脉冲。临时在系统开启「减少动效」确认动画被禁用（可选）。
Expected: 消息逐条淡入上滑；连接灯呼吸。
```bash
git add src/index.css src/components/MessageList.tsx src/components/Toolbar.tsx
git commit -m "feat: add message entrance animation, pulse indicator, reduced-motion support"
```

---

## Task 7: 侧栏选中态滑动指示条

在 Sidebar 层维护「选中项纵向偏移」，用一条绝对定位的强调色竖条平滑滑动。Recent 列表与项目树是两个独立列表，这里实现为：**每个会话列表容器内自带一条指示条**，定位到其选中的 SessionItem。

为最小改动且稳健，采用 CSS-only 方案：选中的 `SessionItem` 自身左侧用 `::before` 伪元素画竖条，配合 `bg-sel-bg` / `text-sel-ink`；hover 用 `translate-x`。滑动感由各项统一的 `transition` + 选中态切换时的视觉连续性近似（真正的跨项滑动需要共享几何，成本高、收益小，YAGNI）。

**Files:**
- Modify: `src/components/SessionItem.tsx`
- Modify: `src/index.css`

- [ ] **Step 1: SessionItem 选中态样式**

将 `src/components/SessionItem.tsx` 整体替换为：
```tsx
import type { SessionInfo } from "../types/message";

interface Props {
  session: SessionInfo;
  isActive: boolean;
  isOpen: boolean;
  onClick: () => void;
}

export default function SessionItem({ session, isActive, isOpen, onClick }: Props) {
  const stateClass = isActive
    ? "cc-session-sel bg-sel-bg text-sel-ink"
    : isOpen
      ? "text-ink bg-accent-soft/40 hover:translate-x-0.5"
      : "text-dim hover:bg-accent-soft hover:text-ink hover:translate-x-0.5";

  return (
    <button
      onClick={onClick}
      className={`relative w-full text-left px-3 py-2 rounded-md text-sm transition-all duration-200 ${stateClass}`}
    >
      <div className="truncate">{session.firstMessage || "(empty)"}</div>
      <div className="text-xs text-dim mt-0.5">
        {new Date(session.timestamp).toLocaleString()} · {session.messageCount} msgs
      </div>
    </button>
  );
}
```

- [ ] **Step 2: 在 index.css 定义选中竖条**

在 `src/index.css` 末尾追加：
```css
/* 侧栏选中态强调竖条 */
.cc-session-sel {
  position: relative;
}
.cc-session-sel::before {
  content: "";
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 3px;
  height: 70%;
  border-radius: 3px;
  background: var(--c-accent);
  animation: cc-bar-in 0.32s cubic-bezier(0.34, 1.3, 0.5, 1);
}
.dark .cc-session-sel::before {
  box-shadow: 0 0 10px 1px var(--c-accent);
}
@keyframes cc-bar-in {
  from { opacity: 0; transform: translateY(-50%) scaleY(0.3); }
  to { opacity: 1; transform: translateY(-50%) scaleY(1); }
}
```

- [ ] **Step 3: 验证 + Commit**

Run: `npm run dev`，点击侧栏不同会话。
Expected: 选中项左侧出现强调色竖条（带弹入动画）+ 背景高亮 + 文字变色；暗色下竖条发光；hover 项轻微右移。
```bash
git add src/components/SessionItem.tsx src/index.css
git commit -m "feat: add sidebar selected-state indicator bar with animation"
```

---

## Task 8: 工具调用接入（后端配对 + 前端渲染）

把已断的线接上：后端补 `tool_result` 配对 + 合并多轮 toolCalls，前端 AssistantMessage 渲染。

**Files:**
- Modify: `server/parser.ts`
- Modify: `src/types/message.ts`
- Modify: `src/components/AssistantMessage.tsx`

- [ ] **Step 1: parser 增加 tool_result 文本提取 + 配对映射**

在 `server/parser.ts` 中：

(a) 修改 `ParsedMessage.toolCalls` 类型，加 `result` 字段：
```ts
  toolCalls?: Array<{
    id: string;
    name: string;
    input: Record<string, unknown>;
    result?: string;
    isError?: boolean;
  }>;
```

(b) 在文件中新增两个辅助函数（放在 `entryToMessage` 之前）：
```ts
function extractToolResultText(content: unknown): string {
  if (typeof content === "string") return content;
  if (Array.isArray(content)) {
    return content
      .map((b) => {
        if (typeof b === "string") return b;
        if (b && typeof b === "object" && "text" in b) return String((b as Record<string, unknown>).text ?? "");
        return "";
      })
      .filter(Boolean)
      .join("\n");
  }
  return "";
}

function buildToolResultMap(entries: RawJsonlEntry[]): Map<string, { text: string; isError: boolean }> {
  const map = new Map<string, { text: string; isError: boolean }>();
  for (const entry of entries) {
    if (entry.type !== "user" || !Array.isArray(entry.message?.content)) continue;
    for (const b of entry.message!.content as Array<Record<string, unknown>>) {
      if (b.type === "tool_result" && typeof b.tool_use_id === "string") {
        map.set(b.tool_use_id, {
          text: extractToolResultText(b.content),
          isError: b.is_error === true,
        });
      }
    }
  }
  return map;
}
```

- [ ] **Step 2: entryToMessage 支持注入 result map，并放宽纯工具消息过滤**

修改 `entryToMessage` 签名与工具调用构造，使其接受可选的 resultMap：
```ts
export function entryToMessage(
  entry: RawJsonlEntry,
  resultMap?: Map<string, { text: string; isError: boolean }>
): ParsedMessage | null {
  if (!entry.uuid || !entry.type || !entry.message) return null;
  if (entry.type !== "user" && entry.type !== "assistant") return null;
  if (entry.isSidechain) return null;
  if (entry.isMeta) return null;

  if (entry.type === "user" && Array.isArray(entry.message.content)) {
    const blocks = entry.message.content as Array<Record<string, unknown>>;
    const hasText = blocks.some((b) => b.type === "text" && typeof b.text === "string");
    if (!hasText) return null;
  }

  const msg: ParsedMessage = {
    uuid: entry.uuid,
    type: entry.type,
    timestamp: entry.timestamp || "",
    content: entry.message.content || "",
    model: entry.message.model,
  };

  if (entry.type === "assistant" && Array.isArray(entry.message.content)) {
    const toolCalls = (entry.message.content as Array<Record<string, unknown>>)
      .filter((b) => b.type === "tool_use")
      .map((b) => {
        const id = (b.id as string) || "";
        const res = resultMap?.get(id);
        return {
          id,
          name: (b.name as string) || "",
          input: (b.input as Record<string, unknown>) || {},
          result: res?.text,
          isError: res?.isError,
        };
      });
    if (toolCalls.length > 0) {
      msg.toolCalls = toolCalls;
    }
  }

  return msg;
}
```

- [ ] **Step 3: mergeMessages 合并 toolCalls，且不丢弃纯工具消息**

修改 `server/parser.ts` 的 `mergeMessages`：
```ts
function mergeMessages(raw: ParsedMessage[]): ParsedMessage[] {
  const merged: ParsedMessage[] = [];

  for (const msg of raw) {
    const prev = merged[merged.length - 1];
    if (prev && prev.type === msg.type) {
      const prevText = extractText(prev.content);
      const curText = extractText(msg.content);
      const combined = [prevText, curText].filter(Boolean).join("\n\n");
      prev.content = combined;
      prev.timestamp = msg.timestamp || prev.timestamp;
      if (msg.type === "assistant") {
        prev.model = msg.model || prev.model;
        if (msg.toolCalls?.length) {
          prev.toolCalls = [...(prev.toolCalls ?? []), ...msg.toolCalls];
        }
      }
    } else {
      merged.push({ ...msg, content: extractText(msg.content) });
    }
  }

  return merged.filter((m) => {
    const text = typeof m.content === "string" ? m.content.trim() : "";
    return text.length > 0 || (m.toolCalls?.length ?? 0) > 0;
  });
}
```

- [ ] **Step 4: parseJsonlFile 与 parseNewLines 接入 resultMap**

在 `parseJsonlFile` 中，解析出所有 entry 后先建 map：
```ts
export function parseJsonlFile(filePath: string): ParsedMessage[] {
  const content = fs.readFileSync(filePath, "utf-8");
  const lines = content.split("\n").filter((l) => l.trim());
  const entries: RawJsonlEntry[] = [];
  for (const line of lines) {
    const entry = parseLine(line);
    if (entry) entries.push(entry);
  }
  const resultMap = buildToolResultMap(entries);
  const raw: ParsedMessage[] = [];
  for (const entry of entries) {
    const msg = entryToMessage(entry, resultMap);
    if (msg) raw.push(msg);
  }
  return mergeMessages(raw);
}
```

在 `parseNewLines` 中同样先收集 entries 再建 map：
```ts
  const entries: RawJsonlEntry[] = [];
  for (const line of lines) {
    const entry = parseLine(line);
    if (entry) entries.push(entry);
  }
  const resultMap = buildToolResultMap(entries);
  const messages: ParsedMessage[] = [];
  for (const entry of entries) {
    const msg = entryToMessage(entry, resultMap);
    if (msg) messages.push(msg);
  }
```
（替换原来 `for (const line of lines)` 直接 `parseLine`→`entryToMessage` 的循环；保留 `return { messages, newOffset: stat.size };`）

- [ ] **Step 5: 前端类型同步**

在 `src/types/message.ts` 的 `ToolCall` 接口加字段：
```ts
export interface ToolCall {
  id: string;
  name: string;
  input: Record<string, unknown>;
  result?: string;
  isError?: boolean;
}
```

- [ ] **Step 6: AssistantMessage 渲染工具调用 + 放宽空文本**

将 `src/components/AssistantMessage.tsx` 整体替换为：
```tsx
import type { Message } from "../types/message";
import { extractTextContent } from "../utils/parseContent";
import MarkdownContent from "./MarkdownContent";
import ToolCallBlock from "./ToolCallBlock";

interface Props {
  message: Message;
}

export default function AssistantMessage({ message }: Props) {
  const text = extractTextContent(message.content);
  const toolCalls = message.toolCalls ?? [];

  if (!text.trim() && toolCalls.length === 0) return null;

  return (
    <div
      id={`msg-${message.uuid}`}
      className="bg-surface border border-edge rounded-lg p-4 my-3 transition-all"
      style={{ fontSize: "var(--font-size)" }}
    >
      <div className="flex items-center gap-2 mb-2">
        <span className="text-xs font-bold text-accent bg-accent-soft px-2 py-0.5 rounded">
          Claude
        </span>
        {message.model && <span className="text-xs text-dim">{message.model}</span>}
        <span className="text-xs text-dim ml-auto">
          {new Date(message.timestamp).toLocaleTimeString()}
        </span>
      </div>

      {text.trim() && (
        <div className="prose max-w-none">
          <MarkdownContent content={text} />
        </div>
      )}

      {toolCalls.map((tc) => (
        <ToolCallBlock key={tc.id} tool={tc} />
      ))}
    </div>
  );
}
```

注：`ToolCallBlock` 的 props 在 Task 9 改为接收 `tool`（含 result）。本任务先建立调用关系，Task 9 完成其内部。若本任务需立即编译通过，可临时让 ToolCallBlock 接受 `tool` 但只用现有字段——但建议 Task 8 与 Task 9 连续执行。

- [ ] **Step 7: 用真实数据验证配对（临时脚本）**

写入 `/tmp/verify_tools.mjs`：
```js
import { parseJsonlFile, getProjectsDir } from "/Users/bytedance/wking/cc-reader/server/parser.ts";
```
（注：parser.ts 是 TS，改用下面的 tsx 方式）

实际运行：
```bash
cd /Users/bytedance/wking/cc-reader && npx tsx -e '
import { parseJsonlFile, getProjectsDir } from "./server/parser.ts";
import * as fs from "fs"; import * as path from "path";
const dir = getProjectsDir();
const proj = fs.readdirSync(dir).find(d => fs.statSync(path.join(dir,d)).isDirectory());
const file = fs.readdirSync(path.join(dir,proj)).find(f => f.endsWith(".jsonl"));
const msgs = parseJsonlFile(path.join(dir,proj,file));
const withTools = msgs.filter(m => m.toolCalls?.length);
console.log("messages:", msgs.length, "with toolCalls:", withTools.length);
const withResult = withTools.flatMap(m=>m.toolCalls).filter(t=>t.result);
console.log("toolCalls total:", withTools.flatMap(m=>m.toolCalls).length, "with result:", withResult.length);
console.log("sample result:", JSON.stringify(withResult[0]?.result?.slice(0,80)));
'
```
Expected: `with toolCalls` > 0，`with result` > 0，sample result 打印出真实输出片段。

- [ ] **Step 8: Commit**

```bash
git add server/parser.ts src/types/message.ts src/components/AssistantMessage.tsx
git commit -m "feat: wire up tool calls with paired tool_result in parser and assistant message"
```

---

## Task 9: 工具块 UI — 显示参数 + 可折叠输出

**Files:**
- Modify: `src/components/ToolCallBlock.tsx`
- Modify: `src/utils/parseContent.ts`

- [ ] **Step 1: getToolSummary 兼容新的 tool 对象**

`src/utils/parseContent.ts` 的 `getToolSummary` 当前接收 `ContentBlock`。改为接收 `{ name, input }` 结构（兼容 ToolCall）：
```ts
export function getToolSummary(block: { name?: string; input?: Record<string, unknown> }): string {
  const name = block.name || "Unknown";
  const input = block.input || {};
  // ...（switch 内容保持不变）
}
```

- [ ] **Step 2: ToolCallBlock 接收 tool（含 result）并渲染折叠输出**

将 `src/components/ToolCallBlock.tsx` 整体替换为：
```tsx
import { useState } from "react";
import type { ToolCall } from "../types/message";
import { getToolSummary } from "../utils/parseContent";

interface Props {
  tool: ToolCall;
}

export default function ToolCallBlock({ tool }: Props) {
  const [expanded, setExpanded] = useState(false);
  const summary = getToolSummary(tool);
  const hasResult = !!tool.result && tool.result.trim().length > 0;

  return (
    <div className="my-2 border border-dashed border-edge rounded-md overflow-hidden">
      <button
        onClick={() => setExpanded(!expanded)}
        className="w-full text-left px-3 py-2 text-sm text-dim hover:text-accent flex items-center gap-2 font-mono transition-colors"
      >
        <span className="text-xs transition-transform duration-200" style={{ transform: expanded ? "rotate(90deg)" : "none" }}>▶</span>
        <span className={tool.isError ? "text-red-400" : "text-accent"}>⚡</span>
        <span className="truncate flex-1">{summary}</span>
        {hasResult && <span className="text-[10px] text-dim shrink-0">output</span>}
      </button>

      <div
        className="transition-all duration-300 ease-in-out overflow-hidden"
        style={{ maxHeight: expanded ? "600px" : "0px" }}
      >
        <div className="px-3 pb-3 text-sm space-y-2">
          <div>
            <div className="text-[10px] uppercase tracking-wide text-dim mb-1">Input</div>
            <pre className="bg-base border border-edge rounded p-3 overflow-x-auto text-ink text-xs leading-relaxed max-h-60 overflow-y-auto">
              {JSON.stringify(tool.input, null, 2)}
            </pre>
          </div>
          {hasResult && (
            <div>
              <div className="text-[10px] uppercase tracking-wide text-dim mb-1">
                {tool.isError ? "Error output" : "Output"}
              </div>
              <pre className={`bg-base border rounded p-3 overflow-x-auto text-xs leading-relaxed max-h-60 overflow-y-auto ${tool.isError ? "border-red-400/40 text-red-300" : "border-edge text-ink"}`}>
                {tool.result}
              </pre>
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 验证 + Commit**

Run: `npm run dev`，打开含工具调用的会话，点击工具块。
Expected: 折叠块平滑展开，显示 Input（参数）与 Output（结果）两段，长输出内部滚动；报错的工具调用红色标记。
```bash
git add src/components/ToolCallBlock.tsx src/utils/parseContent.ts
git commit -m "feat: render tool call input and collapsible output with theme styling"
```

---

## Task 10: 公式渲染（KaTeX + 反引号解包）

**Files:**
- Modify: `src/components/MarkdownContent.tsx`
- Modify: `src/index.css`
- Create: `src/hooks/useIsDark.ts`

- [ ] **Step 1: 创建 useIsDark hook（供代码高亮切换亮/暗主题）**

写入 `src/hooks/useIsDark.ts`：
```ts
import { useState, useEffect } from "react";

export function useIsDark(): boolean {
  const [isDark, setIsDark] = useState(() =>
    document.documentElement.classList.contains("dark")
  );

  useEffect(() => {
    const obs = new MutationObserver(() => {
      setIsDark(document.documentElement.classList.contains("dark"));
    });
    obs.observe(document.documentElement, { attributes: true, attributeFilter: ["class"] });
    return () => obs.disconnect();
  }, []);

  return isDark;
}
```

- [ ] **Step 2: 反引号解包预处理函数**

在 `src/utils/parseContent.ts` 末尾追加导出函数：
```ts
// 把 `$...$` / `$$...$$`（被反引号包裹的纯公式）解包成裸公式，让数学插件接管。
// 仅当反引号内整体就是一段 $...$ 公式时才解包，避免误伤普通行内代码。
export function unwrapInlineMath(text: string): string {
  return text.replace(/`(\${1,2}[^`]*?\${1,2})`/g, (_m, formula) => formula);
}
```

- [ ] **Step 3: MarkdownContent 接入 KaTeX + 解包 + 暗色代码主题**

将 `src/components/MarkdownContent.tsx` 顶部 import 与组件改为：
```tsx
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";
import remarkMath from "remark-math";
import rehypeKatex from "rehype-katex";
import { Prism as SyntaxHighlighter } from "react-syntax-highlighter";
import { oneLight, oneDark } from "react-syntax-highlighter/dist/esm/styles/prism";
import "katex/dist/katex.min.css";
import { useIsDark } from "../hooks/useIsDark";
import { unwrapInlineMath } from "../utils/parseContent";

interface Props {
  content: string;
}

export default function MarkdownContent({ content }: Props) {
  const isDark = useIsDark();
  const processed = unwrapInlineMath(content);

  return (
    <ReactMarkdown
      remarkPlugins={[remarkGfm, remarkMath]}
      rehypePlugins={[rehypeKatex]}
      components={{
        code({ className, children, ...props }) {
          const match = /language-(\w+)/.exec(className || "");
          const code = String(children).replace(/\n$/, "");

          if (match) {
            return (
              <SyntaxHighlighter
                style={isDark ? oneDark : oneLight}
                language={match[1]}
                PreTag="div"
                customStyle={{ fontSize: "var(--font-size)", borderRadius: "6px", margin: "8px 0" }}
              >
                {code}
              </SyntaxHighlighter>
            );
          }
          if (code.includes("\n")) {
            return (
              <div className="bg-base border border-edge rounded-md p-4 my-2 overflow-x-auto">
                <code className="text-sm" style={{ whiteSpace: "pre", display: "block", fontFamily: "inherit" }}>
                  {code}
                </code>
              </div>
            );
          }
          return (
            <code className="bg-accent-soft text-ink px-1.5 py-0.5 rounded text-sm" {...props}>
              {children}
            </code>
          );
        },
        pre({ children }) {
          return <>{children}</>;
        },
        table({ children }) {
          return (
            <div className="overflow-x-auto my-2">
              <table className="border-collapse border border-edge w-full">{children}</table>
            </div>
          );
        },
        th({ children }) {
          return <th className="border border-edge px-3 py-1.5 bg-accent-soft text-left">{children}</th>;
        },
        td({ children }) {
          return <td className="border border-edge px-3 py-1.5">{children}</td>;
        },
        a({ href, children }) {
          return (
            <a href={href} target="_blank" rel="noopener noreferrer" className="text-accent hover:underline">
              {children}
            </a>
          );
        },
      }}
    >
      {processed}
    </ReactMarkdown>
  );
}
```

- [ ] **Step 4: index.css 调整 KaTeX 暗色颜色**

在 `src/index.css` 末尾追加：
```css
.katex { color: var(--c-ink); }
.dark .katex { color: #f1f5f9; }
```

- [ ] **Step 5: 用真实数据验证解包正则（临时脚本）**

Run:
```bash
cd /Users/bytedance/wking/cc-reader && npx tsx -e '
import { unwrapInlineMath } from "./src/utils/parseContent.ts";
console.log(unwrapInlineMath("其 `$M^{s}=\\{x\\}$` 形式"));        // 应解包成 其 $M^{s}=\{x\}$ 形式
console.log(unwrapInlineMath("运行 `npm install` 命令"));         // 不应改变
console.log(unwrapInlineMath("裸 $a+b$ 公式"));                   // 不应改变（本就裸）
console.log(unwrapInlineMath("块 `$$\\frac{1}{2}$$` 测试"));      // 应解包
'
```
Expected: 第 1、4 行反引号被去掉，第 2、3 行不变。

- [ ] **Step 6: 验证渲染 + Commit**

Run: `npm run dev`，打开含 `$...$` 公式的会话（如截图里那个）。
Expected: 行内与块级公式渲染为数学排版；亮/暗主题下公式与代码块颜色均正常。
```bash
git add src/components/MarkdownContent.tsx src/hooks/useIsDark.ts src/utils/parseContent.ts src/index.css
git commit -m "feat: render LaTeX formulas via KaTeX with backtick unwrapping and dark theme support"
```

---

## Task 11: 整体回归验证

**Files:** 无（仅验证）

- [ ] **Step 1: 构建检查**

Run: `npm run build`
Expected: 无 TS 报错，构建产物生成。

- [ ] **Step 2: 全功能截图核对**

Run: `npm run dev`，用 Chrome DevTools 逐项确认：
- [ ] 亮色主题：三栏、消息、工具块、公式均正常
- [ ] 切换到暗色：整页平滑过渡，无撞色，玻璃质感呈现
- [ ] 侧栏选中态竖条 + hover 右移
- [ ] 消息淡入动画
- [ ] 工具块展开显示 Input + Output，长输出滚动
- [ ] 公式渲染（含原灰底反引号公式）
- [ ] 刷新页面主题保持、无白闪

- [ ] **Step 3: 清理临时文件**

确认 `/tmp` 下的验证脚本已无用（无需提交）。`.superpowers/` 已在 gitignore。

- [ ] **Step 4: 最终提交（如有零散改动）**

```bash
git status
# 若有未提交的收尾改动：
git add -A && git commit -m "chore: final polish and cleanup for UI revamp"
```

---

## 自查结论

- **Spec 覆盖**：主题系统(T2-3)、四动画(T6)、侧栏选中态(T7)、工具调用接入(T8)、可折叠输出(T9)、公式渲染(T10)、整体换肤(T4-5,贯穿)、防白闪(T2)、reduced-motion(T6) — 全部有对应任务。
- **类型一致**：`toolCalls[].result/isError` 在 parser(T8-1)、types(T8-5)、ToolCallBlock(T9-2) 三处命名一致；`getToolSummary` 参数放宽以兼容 ToolCall(T9-1)。
- **取舍记录**：侧栏滑动指示条采用 CSS-only 伪元素方案（弹入动画近似滑动），放弃跨项共享几何的复杂实现（YAGNI）；不引入测试框架，逻辑用真实数据脚本验证。
